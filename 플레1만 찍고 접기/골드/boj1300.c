#include <stdio.h>

int main(void) {
    int n, k;
    int start = 0, end;
    int mid;

    scanf("%d %d", &n, &k);
    end = n * n;

    while (start <= end) {
        mid = (start + end) / 2;
        
    }

    return 0;
}